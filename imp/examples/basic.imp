fn basic(x, y) {
	z = x + (y + 1);
	if z == 5 {
		z = 3;
	} else {
		x = 4;
		z = 7;
	}
	while z < 10 {
		z = z + 1;
	}
	return z;
}

fn gcd(a, b) {
	while b != 0 {
		t = b;
		b = a % b;
		a = t;
	}
	return a;
}

fn fma(x, y, z) {
	return x * y + z;
}

fn caller() {
	w = fma(2, 3, 4);
	return w;
}
